# Google Gemini API key used for Imagen image generation.
# NOTE: NEXT_PUBLIC_ variables are embedded in the client bundle and visible
# to anyone who opens the site. For production, proxy image generation
# through a server route instead of exposing the key.
NEXT_PUBLIC_GEMINI_API_KEY=your_api_key_here
